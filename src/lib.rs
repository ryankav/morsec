pub enum MorseChar {
  Dit,
  Dah
};

pub struct Morsec {

};


impl Morsec {
  fn new() -> Self {
    todo!();
  }
}

struct MorseIter {};

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
